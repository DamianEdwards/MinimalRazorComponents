@page "/clientapp"

<LayoutView Layout="typeof(Layout)">
    <h1 class="mt-2">Rendering client components from the server</h1>

    <BlazorWasmApp.App Message="This was initially rendered on the server and then lit up on the client in WebAssembly." />
</LayoutView>